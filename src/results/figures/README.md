To each figure, the some meta-data about the simulation has been added. Thus the meta-data can be read using the following command

    $ sudo apt install imagemagick
    $ identify -verbose image.png
